import 'package:flutter/material.dart';

class VsPlayerScreen extends StatelessWidget {
  const VsPlayerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}